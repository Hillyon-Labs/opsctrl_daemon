apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsctrl-daemon.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opsctrl-daemon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsctrl-daemon.selectorLabels" . | nindent 4 }}
